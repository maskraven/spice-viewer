# scripts

Helper scripts for CI and local development.

| Script | Purpose |
|--------|---------|
| `check_imports.sh` | Enforce package import boundaries (see design doc) |

Interop and milestone scripts will land in later PRs.
